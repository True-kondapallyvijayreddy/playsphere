

old bugs 

Play Sphere - Bugs & Feature Requests
Bug #1: Non-live matches are displayed as Live

Current (Actual):

When clicked events, Matches are showing live which is not live.
Starts only when score board is active or current scoring.
Not completed or Not started.

Expected:

Only matches with an active scoreboard or current scoring should display Live.
Matches that are Not Started or Completed should never be shown as Live.
Bug #2: Matches & Sports navigation

Current (Actual):

Matches & sports → Both buttons are taking to same profile page.

Expected:

Matches should open the Matches page.
Sports should open the Sports page.
Both buttons should navigate independently.
Bug #3: Memory / Photo tagging

Current (Actual):

When a Memory or photo created automatically who are in that should get the tagged and Recorded in Their profile (even umpire).

Expected:

Every detected participant should automatically be tagged.
The memory should appear in every tagged person's profile, including umpires.
Bug #4: Live Matches loading error

Current (Actual):

"Could not load live Matches."

Expected:

Live matches should load correctly without displaying this error.
Bug #5: "Waiting on you" message

Current (Actual):

"Waiting on you" on home screen. What is it?
Create a bell button beside profile Photo on top right, add all these over there.

Expected:

Remove the confusing "Waiting on you" section.
Add a notification bell beside the profile photo.
Move all pending actions and notifications into the bell.
Bug #6: Registration status

Current (Actual):

Once the Registration date is over automatically The event should show "Registration closed".

Expected:

After the registration deadline passes, the event should automatically display Registration Closed.
Bug #7: Rules button placement

Current (Actual):

Remove Rules button from The bottom left of home page, that is not important, it can be under Three lines.

Expected:

Remove the Rules button from the Home screen.
Move it under the hamburger (three-line) menu.
Feature Request #8: Event Creation Flow (Very Very Important)

Current (Actual):

When clicked Create event The first question should be What type of event, which is season, tournament, single match, Challenge other team, etc etc.
Based on This below options will change.

If season is selected:

What are The sports included.
For each sport how many entries.
This can be used for schools, Communities etc.
They can create This and ask for entries.
If They want to add from outside Their clubs They can given option like external entries yes/No.
So They can approve other entries.

If single tournament:

Like badminton or cricket.
They can create and ask for registrations.

Single match:

They can create single match and send That invite to person or club.

Challenge:

They can challenge other Clubs or persons.

Expected:

Event creation should be dynamic.
The first step should always ask the Event Type.
Based on the selected type, show only relevant fields.
Bug #9: Sync issue

Current (Actual):

Even after I have good internet, Matches are waiting to sync for 12 days.

Expected:

Matches should sync immediately when internet connectivity is available.
Bug #10: Single Match details

Current (Actual):

Single Match details are not showing correctly.

Expected:

Display all single match information correctly.
Bug #11: Leaderboard

Current (Actual):

Leader board is not showing up.

Expected:

Leaderboard should load correctly.
Bug #12: Club challenge visibility

Current (Actual):

Member is seeing club challenges.

Expected:

Members should only see challenges intended for them based on permissions.
Bug #13: Logo navigation

Current (Actual):

If clicked on logo, we need to go homepage.

Expected:

Clicking the application logo should always navigate to the Home page.
Feature Request #14: Profile / Sport page

Current (Actual):

If clicked on profile/sport it should show all matches and its scorecards, leader board etc.

Expected:

Profile/Sport page should display:
All matches
Scorecards
Leaderboard
Related statistics
Bug #15: Finished matches still showing Live

Current (Actual):

Even after Match ended like 3-4 days back still showing as "live".
Which not correct.
Ideally when match ended we need to show just scores of each team.
If we click on That we can see full scorecard.

Expected:

Completed matches should never display Live.
Show only the final score summary.
Opening the match should display the complete scorecard.
Feature Request #16: Live Tournament indicator

Current (Actual):

When tournament created, if it is live we need to show blinking light if not both team scores.
If we click on That on going tournament we need to see all These details.
Same goes to season (multi sport).

Expected:

Display a blinking Live indicator for ongoing tournaments.
Show both team scores.
Clicking the tournament should open complete tournament details.
Same behaviour should apply to Seasons (Multi-Sport).
Feature Request #17: Delete Season/Tournament

Current (Actual):

When a season/Tournament Created give option to admin to delete with a reason as option.
And all The Registered Members can get notification.
Same goes to any of The Event.

Expected:

Admin should be able to delete an event.
A reason should be mandatory.
Notify all registered members.
Apply the same functionality to every event type.
Additional Observations
Observation #1: Global Events

Current (Idea):

Created a global Events button under Two Three lines (≡).
For all The tournaments created across The country will be here.
With filters like city, state, sports, type, age etc.
(Open/Closed only)
Users or clubs can search for tournaments and Register if open.

Expected:

Implement a Global Events section with advanced filters and registration support.
Observation #2: Member Page

Current (Idea):

Whats in The member page.

Expected:

Define and improve the Member page experience.
Observation #3: Live matches for members

Current (Actual):

All the live Matches this member part should be shown to him, even he is not registered.

Expected:

Members should be able to view all live matches even if they are not registered.
Observation #4: Club member live scores

Current (Actual):

If a member is playing out side club, for all the other members in the club can see the live scores.

Expected:

Club members should be able to follow live scores of fellow members, even when they play outside the club.
Observation #5: Cheering feature

Current (Idea):

Add cheering button for all the members.
(Think Creatively add This functionality)

Expected:

Introduce a cheering/reaction feature so club members can support players during live matches with creative interactions.

---------------------------------------


NEW bugs

------------------

Bug #1: Season creation error

Current (Actual):

After Creating season for multisports and providing all details, it is saying:
"Could not load the Matches"
"Something went wrong, please try again."

Expected:

After creating a multisport season successfully, it should proceed without showing any error.
The created season should load correctly.
Feature Request #2: Individual & Group Registrations

Current (Actual):

Entries can be done as individual Registration also as team or group.
For example i am a single person i can register as person, later you will attach to Team.
If we are group of people we can register as group so you all will be in Single team.
Register → group/individual → add group members → approval notification to Members → finally admin should also approve it.
Only restricted to inside club, group Must be only club members.

Expected:

Support both Individual and Group registrations.
Allow group creation with member approval flow.
Admin approval should be mandatory.
Groups should contain only club members.
Feature Request #3: Multiple Club Owners

Current (Actual):

A club can be created also.
Club must have a chance to add Multiple owners otherwise it became Monopoly.
A person can give up Their Membership as well as owner status.
If a person removed at least 2/3rd of approval of owners.

Expected:

Clubs should support multiple owners.
Owners should be able to transfer ownership or resign.
Removing an owner should require at least 2/3 owner approval.
Feature Request #4: Start Match Early

Current (Actual):

For example a Match is set for next week Tuesday.
But we wanted start the Match Now.
Then it should start The match by asking few Questions like want to play now?
Same team or any changes.
Same ball etc etc.
Which are relevant to that sport.
And Record all those and start The scorecard.

Expected:

Allow organizers to start scheduled matches early.
Ask sport-specific confirmation questions before starting.
Record responses and begin scorecard.
Bug #5: Live issues

Current (Actual):

Still live issues are There.
Fix all those (SS5).

Expected:

Resolve all remaining live match issues.
Feature Request #6: Toss Utility

Current (Actual):

After team selection toss should come as a utility.
If They done Manually then go by "Record the toss".
If They use ours They can select bat/bowl etc. (Like flip a coin)

Expected:

Provide an in-app toss utility.
Support manual toss recording.
Support coin flip with bat/bowl selection.
Bug #7: Player in multiple lineups

Current (Actual):

In lineups → one player cannot play for both sides.
If player changed to other team he will be removed from this team.
(Applicable for all sports)

Expected:

A player should not be allowed in both teams.
Moving a player should automatically remove them from the previous team.
Bug #8: Striker / Non-striker

Current (Actual):

Stricker & non stricker Cannot be same person.

Expected:

The system should prevent selecting the same player as both striker and non-striker.
Feature Request #9: Cricket Extras

Current (Actual):

Extras → Wide +1/2/3/4.
No ball +1/2/3/4/5/6.
Bye.
Leg runs.
This missing go deep of sport understand the norms and each aspect of The sport.

Expected:

Implement complete cricket extras according to the rules of the sport.
Bug #10: Batter Out Error

Current (Actual):

Instead of Throwing error like this batter is out, simply do not show The name.

Expected:

Do not display players who are already out instead of showing an error.
Feature Request #11: End Innings (Very Important)

Current (Actual):

"End innings" should come even after The last wicket or End of overs.
Cause last wicket or ball go missing.

Expected:

Always provide an End Innings option after the last wicket or completion of overs.
Feature Request #12: Scorecard Layout

Current (Actual):

Score card should be side by side not up and down.
User can select or tap which side They wanted to see.
But for live batting side should show.

Expected:

Display scorecards side-by-side.
Allow switching between teams.
During live matches automatically focus on the batting team.
Feature Request #13: Edge Cases

Current (Actual):

Cover edge case like Tie, Super over etc etc.

Expected:

Support tie, super over and other match edge cases.
Feature Request #14: Lock Match

Current (Actual):

After Match Completed lock The team members, scores.

Expected:

Lock lineups and scores once the match is completed.
Feature Request #15: Edit / Cancel Season

Current (Actual):

If a Season created & wanted to cancel it give option to edit or cancel or send note option so owner can make changes based on situations.

Expected:

Allow owners to edit, cancel or send notes for created seasons.
Feature Request #16: Multi-Sport Club Challenge

Current (Actual):

While sending challenge to other clubs we can send multiple sports at a time.
And multi type like TT single, badminton doubles, cricket leather ball etc etc.

Expected:

Allow challenges containing multiple sports and multiple categories in one invitation.
Feature Request #17: Scoring Permissions

Current (Actual):

We need to set scoring mechanism for who can get access and how.
Owner can do.
Other people can do owner approves.
Or owner can give rights to any of members.

Expected:

Implement role-based scoring permissions.
Support owner-only, owner-approved, and delegated scoring access.
Feature Request #18: Team Selection Approval

Current (Actual):

Other club members They can only choose after approval.
And only They can make changes.
For owner team he can choose.

Expected:

Restrict team selection until approval.
Only authorized users should be able to modify selections.
Bug #19: Scoring Conflict

Current (Actual):

Keep on getting one error "some one else scored this first".

Expected:

Prevent scoring conflicts and handle concurrent updates gracefully without displaying this error.